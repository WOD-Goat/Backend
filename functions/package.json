{
  "name": "wodgoat-functions",
  "version": "1.0.0",
  "main": "lib/index.js",
  "engines": {
    "node": "24"
  },
  "scripts": {
    "build": "tsc",
    "deploy": "yarn build && firebase deploy --only functions",
    "serve": "yarn build && firebase emulators:start --only functions"
  },
  "dependencies": {
    "expo-server-sdk": "^6.1.0",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^18.0.0"
  }
}
